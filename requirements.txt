# Pinewood Derby OpenSource — Python dependencies
# Install: pip install -r requirements.txt
#
# Optional: RPi.GPIO (only available on Raspberry Pi)
#   pip install RPi.GPIO

fastapi>=0.110.0
uvicorn[standard]>=0.29.0
aiofiles>=23.2.1
python-multipart>=0.0.9
aiosmtplib>=3.0.1
qrcode[pil]>=7.4.2
websockets>=12.0

# GPIO support (Raspberry Pi only — comment out on non-Pi systems)
# RPi.GPIO>=0.7.1
